services:
  nginx:
    image: nginx:latest
    container_name: website
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true

    volumes:
      - ./html:/usr/share/nginx/html:ro

    read_only: true
