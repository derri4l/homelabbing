services:
  nginx:
    image: nginx:latest
    container_name: website
    restart: always
    security_opt:
      - no-new-privileges:true
    volumes:
      - ./html:/usr/share/nginx/html:ro
    ports:
      - "8080:80"
